The core module
===============================================================================

The core of the system, which are used as the basis of both the `Editor` and the
`Interpreter` of the system.

This module wraps the following sub-modules:

- :mod:`core.components`


core.components
-------------------------------------------------------------------------------
.. automodule:: core.components
   :members: